package main

import "google-tui/ui"

func main() {
	ui.InitUI()

	/*var list []string

	searcher := search.NewSearcher("Israel", &list)
	err, results := searcher.Search()

	if err != nil {
		log.Fatal(err)
	}

	fmt.Println(cap(results))

	for result := range results {
		fmt.Println(result.Title)
	}

	fmt.Println("Finished!")*/
}
